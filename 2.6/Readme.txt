In 2.6 folder:

participaants_data.csv is the data got from participants.

participant_data.R is used for separating data in participaants_data.csv file.
The output result, individual data, in individual data folder.

refine.R is used for respectively running the file in individual data folder to generate the result(result_data.csv).
The output is result_data.csv file.

output.R is used for generating descriptive data, histograms and boxplots.
You can find descriptive data, histograms and boxplots in this folder. 